import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { MaxikingPage } from './maxiking';

@NgModule({
  declarations: [
    MaxikingPage,
  ],
  imports: [
    IonicPageModule.forChild(MaxikingPage),
  ],
})
export class MaxikingPageModule {}
